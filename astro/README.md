# astro

``` bash
sudo apt install python3-virtualenv -y
sudo apt install python3-venv -y
sudo apt install python3-dev graphviz libgraphviz-dev pkg-config -y
python3 -m venv env
source env/bin/activate
pip install numpy
pip install opencv-python
pip install matplotlib
pip install astropy
pip install pyqt5
```
